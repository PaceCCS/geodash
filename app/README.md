# Tauri App

Rust shell hosting a WebGPU frontend.

- Network editor UI (create/edit scopes, visualize the DAG)
- Phase diagram plotter (ONNX neural nets → RGBA storage textures)
- Steady-state pipe simulation using thermodynamic property textures
- dim WASM build for client-side unit display and conversion
